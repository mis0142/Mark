[build]
  publish = "."
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- . ':!portal'"

[dev]
  publish = "."

# Keep portal/ available under /portal for convenience, but primary portal is separate site.
